---
interface Props {
  title: string;
}
const { title } = Astro.props;
---

<h2 class="text-3xl md:text-4xl font-bold text-text text-center mb-2">{title}</h2>
<div class="w-16 h-1 bg-primary mx-auto mb-10 rounded"></div>
